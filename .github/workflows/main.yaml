on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types: [opened, synchronize]
# test
jobs:
  helm-static-code-analysis:
    uses: k-ngs-test/gh-workflows/.github/workflows/_helm_static_analysis_template.yaml@main
  kustomize-static-code-analysis:
    uses: k-ngs-test/gh-workflows/.github/workflows/_kustomize_static_analysis_template.yaml@main
    with:
      kustomize-path: "./base"