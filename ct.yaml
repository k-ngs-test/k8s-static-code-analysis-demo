# See https://github.com/helm/chart-testing#configuration
remote: origin
target-branch: main
# Monorepoの場合はここでPathを追加する
chart-dirs:
  - charts
# chartsを外部レポジトリから取得する場合に必要
# chart-repos:
#   - bitnami=https://charts.bitnami.com/bitnami
helm-extra-args: --timeout 600s